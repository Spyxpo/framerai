# Development and CI tooling. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest>=8.4.0
pytest-cov>=7.0.0
ruff>=0.14.0

# safetensors and onnx are optional runtime extras, but export paths are covered by
# CI, so they are not optional here.
safetensors>=0.7.0
onnx>=1.16.0
onnxruntime>=1.18.0
